apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-scripts
  namespace: boutique
data:
  data:
  10-create-databases.sql: |
    CREATE DATABASE auth_db;
    CREATE DATABASE products_db;
    CREATE DATABASE orders_db;
    CREATE DATABASE users_db;

  20-init-schema.sql: |
    -- Example: Create tables in auth_db
    \c auth_db
    CREATE TABLE IF NOT EXISTS users (
      id SERIAL PRIMARY KEY,
      name VARCHAR(100),
      email VARCHAR(100) UNIQUE
    );

  quick-seed.sql: |
    -- Seed data for demo
    \c auth_db
    INSERT INTO users(name, email) VALUES('darshan','darshanchaudhari4545@gmail.com'),('darsh','darshanchau9090@gmail.com');
